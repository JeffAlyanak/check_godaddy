# Godaddy Check

```bash
Usage of ./godaddy-check:
  -crit int
        days until critical (default 7)
  -domain string
        domain to search
  -key string
        API Key
  -secret string
        API Secret
  -warn int
        days until warning (default 15)
```